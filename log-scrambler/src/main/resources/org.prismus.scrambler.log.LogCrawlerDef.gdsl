/**
 * @author Serge Pruteanu
 */
contributor context(scope: scriptScope(name: ~/(?:.+log).groovy/)), {

    findClass('org.prismus.scrambler.log.LogCrawler.Builder')?.methods?.each {
        if (!it.isConstructor() && 'build' != it.name) {
            add it
        }
    }

}
