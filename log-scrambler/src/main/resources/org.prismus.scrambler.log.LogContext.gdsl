/**
 * @author Serge Pruteanu
 */
contributor context(scope: scriptScope(name: ~/(?:.+log).groovy/)), {

    findClass('org.prismus.scrambler.log.LogScrambler.Builder')?.methods?.each {
        if (!it.isConstructor()) {
            add it
        }
    }

}
